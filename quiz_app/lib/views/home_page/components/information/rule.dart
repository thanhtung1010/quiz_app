import 'package:flutter/material.dart';

class Rule extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Your history score here...'),
    );
  }
}
